// Servicio de notificaciones simplificado
// Las notificaciones ahora se manejan completamente a través de Supabase
// Este servicio se mantiene como stub para compatibilidad
export const NotificationService = {
  initialize: async () => {
    console.log('🟢 NotificationService inicializado (Supabase mode)');
    // No se requiere inicialización adicional
  }
};
